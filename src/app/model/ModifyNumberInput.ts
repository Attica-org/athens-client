export interface Participants {
  maxCnt: number;
  minCnt: number;
  maxCntMessage: string;
  minCntMessage: string;
  defaultCnt: number;
  decreaseAriaLabel: string;
  increaseAriaLabel: string;
  inputAriaLabel: string;
}
