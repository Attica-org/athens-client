"use client";

import { usePathname } from "next/navigation";

export default function AgoraUserList() {
  return (
    <section className="flex-1 flex-col h-dvh bg-blue-600 max-w-15rem flex-grow hidden lg:block">
      <h1>유저 리스트입니다 여기는 유저 리스트 입니다.</h1>
    </section>
  );
}
